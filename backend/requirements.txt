# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Database
motor==3.4.0
pymongo==4.7.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Config
pydantic-settings==2.3.0
pydantic[email]>=2.7.4

# File processing
PyMuPDF==1.24.5
python-docx==1.1.2

# Embeddings
openai==2.41.0
httpx>=0.27.0
# sentence-transformers intentionally excluded:
# It pulls PyTorch (~800MB) which exceeds Render's 512MB RAM limit.
# Local embeddings are disabled. Set EMBEDDING_STRATEGY=openai in env.

# Vector store: MongoDB Atlas Vector Search (free M0 tier)
# chromadb removed — it stored data on Render's ephemeral disk (lost on redeploy)
# Atlas Vector Search uses motor/pymongo already listed above — no extra package

# Utilities
python-dotenv==1.0.1
langchain-text-splitters==1.1.2
